/*
 * @features global types are commons types throughout feature directory only
 * */

/*
 * LoadingStatus is the action of current request lifecycle
 *  */
export type LoadingStatus = 'idle' | 'pending' | 'successful'

/*
 * return predefined state from api to handle any errors
 * */
export type ApiState = ''
